// Fetch weather from our protected endpoint, never directly with a provider key.
// Later: accept a location or coordinates and an AbortSignal; return raw JSON.
// Keep DOM updates, normalization, and persistence outside this module.
export {};
