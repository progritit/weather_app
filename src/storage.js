// Persist preferences, saved/default locations, and timestamped weather cache.
// Later: validate/version stored data and tolerate unavailable localStorage.
// Never store the provider key; persist precise geolocation only with consent.
export {};
