// Map normalized weather and local day/night to icons, themes, and hero assets.
// Landscape choice is separate: weather data cannot establish land use.
// Use explicit imports or a bounded asset map so Webpack includes the images.
export {};
